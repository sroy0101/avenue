version: "3.9"
services:
  web:
    build: ./backend
    ports:
    - "8000:8000"
    command: python manage.py runserver 0.0.0.0:8000
    volumes:
      - .:/backend
  client:
    stdin_open: true
    build: ./frontend
    ports:
      - "3000:3000"
    volumes:
      - .:/frontend